# Blender Android File Viewer

Android app for selecting `.blend` files and copying them into private app storage. It provides file management and a viewer handoff; it does not bundle the Blender renderer.

```sh
./gradlew assembleDebug
```
